package com.chwipoClova.resume.repository;

import com.chwipoClova.resume.entity.ResumeTemp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ResumeTempRepository extends JpaRepository<ResumeTemp, Long> {
}